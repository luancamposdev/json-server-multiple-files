{
  "name": "json-server-multiple-files",
  "version": "1.0.0",
  "description": "Json-server multiple files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./fakeapis/index.js --port 3333"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Luan Campos",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.14.0"
  }
}
